export interface Termo {
  tipoTermo: 'responsabilidade' | 'devolucao';
  nome: string;
  cargo: string;
  unidade: string;
  registroCrea: string;
  patrimonio: string;
  equipamento: string;
  dataEmissao: string;
}